namespace Terumi.Ast
{
	// `MemberDefinition`s are for things that are half implemented
	// anything that's a Member is already a MemberDefinition though
	// so MethodDefinition (eg. `some_method(string a)`)
	public class MemberDefinition
	{
	}
}